# Believe Chops API (Express backend). Defaults to http://localhost:4000 if unset.
VITE_API_URL=http://localhost:4000

# Public URL of this React app (no trailing slash). Omit locally; set on Render if you use a custom domain.
# Production default when unset: https://believechops.onrender.com
# VITE_SITE_URL=https://believechops.onrender.com
